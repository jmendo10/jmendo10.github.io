var person = prompt("Please enter your name", ""); 
if (person == null) { 
	return "that's a bad name or whatever you want if no value is entered."; 
} 
 if (person == "shane") { 
   return "that's bad good name"; 
 } 
  return "that's a good name"; 
